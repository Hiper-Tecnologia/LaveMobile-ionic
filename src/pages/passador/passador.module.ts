import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PassadorPage } from './passador';

@NgModule({
  declarations: [
    PassadorPage,
  ],
  imports: [
    IonicPageModule.forChild(PassadorPage),
  ],
})
export class PassadorPageModule {}
